<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />
            <!-- 配置数据库连接信息 -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver" />
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis" />
                <property name="username" value="root" />
                <property name="password" value="" />
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!-- 注册userMapper.xml文件，
        userMapper.xml位于me.gacl.mapping这个包下，所以resource写成me/gacl/mapper/userMapper.xml-->
        <mapper resource="models/dao/mapping/userMapper.xml"/>
        <mapper resource="models/dao/mapping/orderMapper.xml"/>
        <mapper resource="models/dao/mapping/classMapper.xml"/>
        <!-- 注册UserMapper映射接口-->
        <mapper class="models.dao.mapping.UserMapperI"/>
    </mappers>
</configuration>